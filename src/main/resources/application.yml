spring:
  application:
    name: post

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: "jdbc:mysql://13.124.119.180:3306/example_db?autoReconnect=true&useUnicode=true&serverTimezone=Asia/Seoul"
    username: hsw
    password: hsw
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        storage_engine: innodb
        format_sql: true
        use_sql_comments: true
    hibernate:
      ddl-auto: create
    open-in-view: false
    show_sql: true